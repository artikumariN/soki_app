'use strict';

var modulename = 'utils';



    var fullname = namespace + '.' + modulename;

    var appUtil = angular.module('utils', ['ui.router','ionic', 'ngCordova']);
    //appUtil.namespace = appUtil.namespace || {};
